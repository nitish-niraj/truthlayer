# Drop JSON dumps of `/api/verify*` responses here.

Use the curl recipes in `../README.md` to generate these. Sanitize
sensitive fields (`request_id`, `source_url`s you do not want
redistributed, raw LLM reasoning text) before publishing.
